rootProject.name = "vela"

// Plugins
include(
    "plugin-backgrounds",
    "plugin-buildconfig",
    "plugin-keygen",
    "plugin-versioning"
)

// Tools
include(
    "tool-epc"
)

// Libraries
include(
    "lib-transfervalue"
)
