/** CLASS GENERATED BY VELA-Plugin
 *
 * CLASS USED FOR EASIER GENERATION
 */
public class GeneratedKeyBase {
    /** CLASS GENERATED BY VELA-Plugin
     *
     * INTERNAL VALUE
     */
    protected final String BASE;

    GeneratedKeyBase(String key, String parent) {
        if(parent.isBlank()){
            BASE = key;
        } else {
            BASE = parent + "{{PLACEHOLDER}}" + key;
        }
    }

    @Override
    public String toString() {
        return BASE;
    }
}
